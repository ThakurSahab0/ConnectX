{
  "dependencies": {
    "@zegocloud/zego-uikit-prebuilt": "^1.8.0",
    "express": "^4.18.2"
  },
  "scripts": {
    "server": "nodemon server/App.js",
    "client": "npm start --prefix connectx",
    "test": "echo \"Error: no test specified\" && exit 1",
    "vikas": "concurrently \"npm run server\" \"npm run client\""
  }
}
